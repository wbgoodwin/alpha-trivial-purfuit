beforeAll(() => {
    //return initializeDatabase();
});

afterAll(() => {
    //return clearDatabase();
});

test('SQL dump importer', () => {});

test('SQL dump exporter', () => {});

test('get game questions', () => {});

test('get game categories', () => {});

test('read a question', () => {});

test('update category name', () => {});

test('update category color', () => {});

test('add new question', () => {});

test('delete question', () => {});